<?xml version="1.0" encoding="utf-8"?>
<!--
    Top-left blurred background circle (Figma node 16:27).
    Figma: 260dp white circle @ ~10% opacity with a large layer blur.

    Android has no SVG blur filters, so the soft "out of focus" disk is
    reproduced with a radial gradient that fades from the fill opacity at the
    centre to fully transparent at the rim. Resolution-independent, no bitmap,
    no blur pass — works on minSdk 24+.

    Host View is @dimen/splash_circle_top_size (340dp = 260 circle + blur
    falloff) and is centred on the circle centre in the layout.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="radial"
        android:centerX="0.5"
        android:centerY="0.5"
        android:gradientRadius="170dp"
        android:startColor="#1AFFFFFF"
        android:centerColor="#14FFFFFF"
        android:endColor="#00FFFFFF" />
</shape>
