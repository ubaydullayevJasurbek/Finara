<?xml version="1.0" encoding="utf-8"?>
<!-- Sign In primary CTA: brand-green gradient, pill shape (login-scoped). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="@color/brand_green"
        android:endColor="@color/brand_green_dark"
        android:angle="0" />
    <corners android:radius="@dimen/login_button_corner" />
</shape>
